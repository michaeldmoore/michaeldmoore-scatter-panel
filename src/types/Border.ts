/* eslint no-useless-constructor:off, no-unused-vars:off, no-empty-function:off */
export class Border {
  constructor(
    public show: boolean,
    public color: string,
    public size: number,
  ) {
  }
}
